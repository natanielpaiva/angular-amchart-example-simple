{
  "name": "angular-am-chart",
  "version": "0.0.0",
  "description": "Um projeto para facilitar o uso da lib AmChart com o Angular",
  "repository": "https://github.com/natanielpaiva/angularAmCharts",
  "devDependencies": {
    "bower": "^1.3.1",
    "http-server": "^0.6.1"
  },
  "scripts": {
    "postinstall": "bower install",

    "prestart": "npm install",
    "start": "http-server -a localhost -p 8000 -c-1"
  }
}
